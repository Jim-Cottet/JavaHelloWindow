package com.jim;

import javax.swing.*;

public class MainFrame extends JFrame {
    public void init() {
        setTitle("Hello every body");
        setSize(450, 300);
        setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
        setVisible(true);
    }
}
